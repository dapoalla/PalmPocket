# Local database credentials, generated by setup.php - never commit this
includes/config.local.php

# Legacy flat-file data (superseded by the database)
data/

# Old iterations kept around locally, not part of the app
old4/
expenses/
expenses.zip

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
